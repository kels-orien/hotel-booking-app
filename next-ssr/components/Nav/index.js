import styles from './Nav.module.scss'

const Nav = () => {
    return (
        <div className={styles.error, styles.navbar, styles.navbarModify}>
            <h1>Nav</h1>
        </div>
    )
}

export default Nav
